# 설정파일 - xconfig 전체를 제외하고 양식(.example)만 추적
xconfig/*
!xconfig/*.example

# 자동생성 - 접근토큰 (kiwoomKey.json 으로 발급받아 xapi 폴더에 캐싱)
xapi/kiwoomToken.json
xapi/kiwoomToken.lock
xapi/kakaoToken.json

# Python 캐시
__pycache__/
*.py[cod]
*.egg-info/

# 가상환경
.venv/
venv/

# OS / IDE
.DS_Store
.idea/
.vscode/

# 작업 지침 (로컬 전용 - 저장소가 공개라 경로 정보를 올리지 않는다)
CLAUDE.md
